#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

# Run linting
echo "🔍 Running ESLint..."
yarn lint

# Run TypeScript check
echo "🔍 Running TypeScript check..."
yarn type-check

echo "✅ Pre-commit checks passed!"
